#!/bin/bash
# LocalStack initialization script.
# This script runs automatically when LocalStack is fully ready.
# Use it to pre-create any resources that tests depend on, or leave it empty.

echo "LocalStack is ready — S3 and SQS are available on http://localhost:4566"
